#include"main.h"

/**
*_abs - function computes the absolute value
*of an integer
*@i: the integer to be computed
*
*Return: Always 0
*/
int_abs(int i)
{
if (i < 0)
{
	return (i *= -1);
}
else
	return (i);
}
